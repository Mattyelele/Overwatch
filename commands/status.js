const commando = require('discord.js-commando');
const Discord = require('discord.js');


module.exports = {
    name: 'status',
    description: 'Can change the bots status through the !status command',
    execute(message, args){
        
    }
}